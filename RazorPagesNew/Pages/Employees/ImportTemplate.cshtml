@page "{format}"
@model RazorPagesNew.Pages.Employees.ImportTemplateModel
@{
    // Этот файл не будет использоваться напрямую, так как страница будет отправлять файл.
    // Но он нужен для соответствия соглашениям Razor Pages.
}